def find_union(a, b):
    # Convert both arrays to sets and take union
    union_set = set(a) | set(b)
    
    # Convert back to list (any order allowed)
    return list(union_set)


# Example usage
a = [1, 2, 3, 2, 1]
b = [3, 2, 2, 3, 3, 2]

result = find_union(a, b)
print(result)
